name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: xcodebuild test -project PlaySpot.xcodeproj -scheme PlaySpot -destination 'platform=macOS' -resultBundlePath TestResults
